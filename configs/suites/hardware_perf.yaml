schema_version: "1.0"
id: suite.hardware.performance.001
name: Hardware performance
description: >
  Hardware/performance warm run measuring TTFT, decode throughput and per-node
  telemetry over deterministic perf probes (spec 8.1E, 15, Phase 3 gate).
version: 1
family: hardware_performance

tasks:
  - task_id: hardware.perf.probe.001
    weight: 1.0
    repetitions: 3
