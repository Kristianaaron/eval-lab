schema_version: "1.0"
id: reasoning.reverse_string.001
name: 'Reverse a string'
description: 'Return the input string reversed, preserving case.'
version: 1
level: model
status: active

labels:
  domains: ['general_reasoning', 'coding']
  capabilities: ['instruction_following']
  modalities: ['text']
  difficulty: easy
  trajectory_stages: ['execute']
  failure_modes_targeted: []
  intervention: []
  atlas_labels: []

input:
  instruction_file: 'prompt.md'
  attachments: []
  workspace_fixture: null
  initial_state_hash: null

execution:
  runner: direct
  sandbox: null
  network: disabled
  timeout_seconds: 300
  max_turns: null
  max_tool_calls: null
  allowed_tools: []
  seeds: [0]

oracle:
  - type: exact
    weight: 1.0
    required: true
    config: {"expected": "olleh"}
repetitions:
  default: 1
  seeds: []
