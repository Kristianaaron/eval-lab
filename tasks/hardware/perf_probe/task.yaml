schema_version: "1.0"
id: hardware.perf.probe.001
name: 'Deterministic perf probe'
description: 'Emit a fixed marker to benchmark generation latency and throughput.'
version: 1
level: model
status: active

labels:
  domains: ['hardware']
  capabilities: ['instruction_following']
  modalities: ['text']
  difficulty: trivial
  trajectory_stages: ['execute']
  failure_modes_targeted: []
  intervention: []
  atlas_labels: []

input:
  instruction_file: 'prompt.md'
  attachments: []
  workspace_fixture: null
  initial_state_hash: null

execution:
  runner: direct
  sandbox: null
  network: disabled
  timeout_seconds: 300
  max_turns: null
  max_tool_calls: null
  allowed_tools: []
  seeds: [0]

oracle:
  - type: regex
    weight: 1.0
    required: true
    config: {"pattern": "deterministic-reply"}
repetitions:
  default: 1
  seeds: []
