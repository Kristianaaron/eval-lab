schema_version: "1.0"
id: mathematics.prime_factorization.001
name: 'Prime factorization'
description: 'Factor a number into primes and return the sorted factor list.'
version: 1
level: model
status: active

labels:
  domains: ['mathematics']
  capabilities: ['mathematical_reasoning']
  modalities: ['text']
  difficulty: medium
  trajectory_stages: ['execute']
  failure_modes_targeted: []
  intervention: []
  atlas_labels: []

input:
  instruction_file: 'prompt.md'
  attachments: []
  workspace_fixture: null
  initial_state_hash: null

execution:
  runner: direct
  sandbox: null
  network: disabled
  timeout_seconds: 300
  max_turns: null
  max_tool_calls: null
  allowed_tools: []
  seeds: [0]

oracle:
  - type: json_schema
    weight: 1.0
    required: true
    config: {"schema": {"type": "object", "properties": {"factors": {"type": "array", "items": {"type": "integer"}}}, "required": ["factors"]}}
repetitions:
  default: 1
  seeds: []
