schema_version: "1.0"
id: agentic.api_migration.001
name: 'Dependency API migration'
description: 'Update code to a new library API version across a workspace.'
version: 1
level: model
status: active

labels:
  domains: ['agentic', 'coding']
  capabilities: ['repository_navigation', 'code_editing', 'code_generation', 'error_interpretation']
  modalities: ['code']
  difficulty: hard
  trajectory_stages: ['inspect', 'plan', 'modify', 'execute', 'verify']
  failure_modes_targeted: []
  intervention: []
  atlas_labels: []

input:
  instruction_file: 'prompt.md'
  attachments: []
  workspace_fixture: 'workspace'
  initial_state_hash: null

execution:
  runner: agent
  sandbox: null
  network: disabled
  timeout_seconds: 300
  max_turns: 10
  max_tool_calls: null
  allowed_tools: []
  seeds: [0]

oracle:
  - type: unit_test
    weight: 1.0
    required: true
    config: {"command": "python tests/test_app.py", "workspace": "", "timeout_seconds": 120}
repetitions:
  default: 1
  seeds: []
