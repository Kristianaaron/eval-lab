schema_version: "1.0"
id: voxel.lattice_path.001
name: 'Count 3D lattice paths'
description: 'Count monotone lattice paths avoiding blocked voxels.'
version: 1
level: model
status: active

labels:
  domains: ['voxel', 'spatial_3d']
  capabilities: ['spatial_reasoning', 'mathematical_reasoning']
  modalities: ['structured']
  difficulty: hard
  trajectory_stages: ['execute']
  failure_modes_targeted: []
  intervention: []
  atlas_labels: []

input:
  instruction_file: 'prompt.md'
  attachments: []
  workspace_fixture: null
  initial_state_hash: null

execution:
  runner: direct
  sandbox: null
  network: disabled
  timeout_seconds: 300
  max_turns: null
  max_tool_calls: null
  allowed_tools: []
  seeds: [0]

oracle:
  - type: regex
    weight: 1.0
    required: true
    config: {"pattern": "^\\d+$"}
repetitions:
  default: 1
  seeds: []
