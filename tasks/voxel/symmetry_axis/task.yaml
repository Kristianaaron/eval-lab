schema_version: "1.0"
id: voxel.symmetry_axis.001
name: 'Check voxel symmetry plane'
description: 'Determine the plane about which a voxel set is symmetric.'
version: 1
level: model
status: active

labels:
  domains: ['voxel', 'spatial_3d']
  capabilities: ['voxel_construction', 'spatial_reasoning']
  modalities: ['structured']
  difficulty: hard
  trajectory_stages: ['inspect', 'execute']
  failure_modes_targeted: []
  intervention: []
  atlas_labels: []

input:
  instruction_file: 'prompt.md'
  attachments: ['data/input.json']
  workspace_fixture: null
  initial_state_hash: null

execution:
  runner: direct
  sandbox: null
  network: disabled
  timeout_seconds: 300
  max_turns: null
  max_tool_calls: null
  allowed_tools: []
  seeds: [0]

oracle:
  - type: exact
    weight: 1.0
    required: true
    config: {"expected": "{\"plane\": \"x0\"}"}
repetitions:
  default: 1
  seeds: []
