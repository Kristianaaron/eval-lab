# eval-lab

Agent-agnostic local model and agent evaluation harness.

Phase 0 scaffold: versioned schemas, label registry, YAML task loader,
CLI (`doctor`, `validate`, `list`), and a deterministic mock model adapter.

See `docs/architecture.md`, `docs/data-contracts.md`, and `docs/adr/`.
See `PHASE_0_REPORT.md` for the Phase 0 exit-gate evidence.
